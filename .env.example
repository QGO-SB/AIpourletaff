# --- Supabase (Projet > Settings > API) ---
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

# --- Orchestrateur (serveur maison multi-utilisateurs, voir infra-home/) ---
# Ne JAMAIS préfixer ces 2 variables par NEXT_PUBLIC_ : elles ne doivent
# être lues que côté serveur (dans app/api/vm-access/route.ts).
ORCHESTRATOR_URL=https://qgo-orch.duckdns.org
ORCHESTRATOR_SECRET=changeme

# --- (Ancien système mono-utilisateur, VM Google Cloud, voir infra/) ---
# Plus utilisé par l'app depuis le passage au multi-utilisateurs, mais la VM
# reste disponible directement en dépannage perso (iabe.duckdns.org).
